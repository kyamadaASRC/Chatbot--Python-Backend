const template_files = [
    {
        file_name: "Lesson plan.docx",
        file_id: "file-VCxw76eajksHxgh71g7JK9", //We upload this file to OpenAI using File API then hardcode the value into our chatbot here
        //file_info generated by Resoponse API and hardcoded so we don't have to run the llm on boot. We will need to make a separate application only for developer use to generate the file_info of the docx
        file_info:`# ✅ **Document Description**

The uploaded **Lesson plan.docx** is a structured lesson plan for a **Math – 1st Period** class.
It contains standard lesson-plan sections with placeholder or lorem-ipsum content.

### **Main Sections Present**

* **Header Info**

  * Subject / Class period
  * Lesson Plan title
  * Teacher name
  * Date
* **Overview & Purpose**
  Contains placeholder lorem ipsum text.
* **Education Standards**
  Contains bulleted / enumerated items with lorem ipsum–style placeholders.
* **Objectives**
  Three high-level learning objectives (also placeholders).
* **Materials Needed**
  Two items with counts.
* **Verification** (Assessment / Understanding Check)
  Three lines describing checks for understanding.
* **Activity**
  One paragraph describing planned activity (placeholder text).

The document has **no actual DOCX fillable fields** (no content controls, no merge fields).
However, the structure is consistent, so we can define *logical fields* for automation.

---

# ✅ **Inferred Fillable Fields (for future Code Interpreter automation)**

These represent **semantic fields** that Code Interpreter can fill when modifying the template:

### **Header Fields**

* \`subject\`
* \`class_period\`
* \`lesson_title\`
* \`teacher_name\`
* \`date\`

### **Overview Section**

* \`overview_purpose\`

### **Education Standards (list)**

* \`standards = [standard_1, standard_2, standard_3, ...]\`

### **Objectives (list)**

* \`objectives = [objective_1, objective_2, objective_3, ...]\`

### **Materials Needed**

* \`materials = [ { "item": "...", "quantity": ... }, ... ]\`

### **Verification / Assessment**

* \`verification_steps = [step_1, step_2, step_3]\`

### **Activity Description**

* \`activity_description\``
    }
]